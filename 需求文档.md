# 个人旅游足迹地图 MVP 需求文档

## 1. 项目概述

### 1.1 产品目标

构建一个本地运行的个人旅游足迹地图 Web 应用，用地图展示用户去过的景点和体验过的美食，并集中管理图片、攻略、结构化信息和个人评价。核心目标是让用户在 10 分钟内完成一趟旅行中多个景点的快速录入，并能通过地图和筛选快速回顾。

### 1.2 用户与角色

- 管理员：本地部署初始化账号，可管理用户账号和全部内容。
- 普通用户：注册、登录，只能查看和维护自己的内容。

当前 MVP 内容仅对所属用户可见，不提供好友浏览和公开分享。

### 1.3 MVP 范围

包含：账号注册/登录、景点和美食点录入、图片原图及缩略图、本地地图展示、搜索定位、筛选、评价编辑、评价历史查看、草稿/发布/归档、永久删除、内容管理列表。

不包含：真正离线编辑、好友关系、多用户公开分享、社交互动、3D 地图、路线导航、数据导出、版本对比与旧版本恢复、手机端上传。

## 2. 技术方案

- 前端：Vue 3 + Vite + Vue Router + Pinia。
- 后端：Java 17+、Spring Boot、Spring Security、JPA 或 MyBatis（二选一并统一）。
- 数据库：MySQL 8.x。经纬度建立索引，后续全球化可升级空间类型/空间索引。
- 地图：Leaflet + OpenStreetMap 二维底图，显示 OSM 署名。地点搜索优先使用 Nominatim；受公共服务频率限制时，可切换为手工经纬度或高德个人开发者免费额度。
- 图片：本地文件系统保存原图和自动生成的缩略图，数据库保存元数据及相对路径。目录建议为 `uploads/{userId}/{contentType}/{contentId}/`。
- Redis：MVP 暂不使用。未来可用于会话、限流、缓存和异步任务队列。
- 部署：本机 `localhost` 运行，前后端分离开发，构建后可由 Spring Boot 或 Nginx 提供静态文件。

## 3. 功能需求

### 3.1 认证与账号

- 用户名 + 密码注册和登录。
- 用户名 4-20 位，允许字母、数字、下划线；密码至少 8 位。
- 支持退出登录、修改密码。
- 管理员可查看、启用/禁用和删除普通用户。

### 3.2 我的地图

- 左侧筛选区和内容列表，右侧大幅二维地图。
- 默认展示当前用户所有“已发布”景点和美食点；草稿、归档默认隐藏。
- 支持按国家、省、市、到访日期筛选。
- 支持按名称、地址、攻略和美食店名搜索。
- 列表项点击后地图定位并打开详情；地图点位悬浮显示预览卡片，点击进入详情。
- 景点和美食使用不同图标/颜色区分。
- 提供“按到访日期连线”开关；开启后按日期顺序连接当前筛选结果，允许关闭。
- 点位达到数百以上时引入聚合显示，MVP 可先实现基础点位。

### 3.3 景点录入向导

步骤固定为：基本信息 → 图片 → 旅游攻略 → 当地美食 → 个人评价 → 保存草稿/发布。

基本信息：

- 必填：景点名称、经纬度、单日到访日期。
- 输入名称后调用地点搜索，展示名称、城市、地址候选；用户确认候选后填充坐标和地址，可在地图上微调。
- 国家、省、市由反向地理编码自动填充，用户可修改。
- 支持状态：草稿、已发布、已归档。

图片：

- 单个景点最多 10 张，单张最大 20MB。
- 支持电脑端选择、拖拽和粘贴上传；原图保留，并自动生成用于列表/地图的缩略图。
- 无 EXIF GPS 时不影响上传；存在 GPS 时仅作为位置建议，必须由用户确认后采用。

攻略：

- 支持文字内容。
- 支持多个外部链接。

当地美食：

- 可在景点向导中新增多个美食条目，也可之后独立创建。
- 美食在地图上作为独立点显示，可关联某个景点或完全独立存在。
- 最小字段：店名、地址、经纬度。可选字段：图片、价格、评分、营业时间、链接、备注。

个人评价：

- 预设字段：星级（1-5）、文字评价、优点、缺点、推荐指数（1-5）。全部可选。
- 支持预设字段开关，用户可隐藏不使用的字段。
- 每次保存评价生成一条历史版本；MVP 详情页仅支持查看历史，不做版本对比和恢复。

### 3.4 内容管理

- 管理列表展示景点和美食，支持按状态、地区、日期筛选。
- 支持编辑、发布、保存草稿、归档和永久删除。
- 删除采用二次确认；景点删除时级联永久删除关联图片、美食关联关系、评价及评价历史。
- 归档内容默认不在地图展示，但保留在管理列表，可恢复为草稿或发布。

## 4. 接口边界

认证：`POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/logout`、`PUT /api/auth/password`。

景点：`GET/POST /api/places`、`GET/PUT/DELETE /api/places/{id}`、`POST /api/places/{id}/publish`、`POST /api/places/{id}/archive`。

美食：`GET/POST /api/foods`、`GET/PUT/DELETE /api/foods/{id}`。

图片：`POST /api/media`、`DELETE /api/media/{id}`，支持原图和缩略图 URL。

评价：`GET/PUT /api/places/{id}/review`、`GET /api/reviews/{id}/versions`。

地图搜索：`GET /api/geocode/search?q=`、`GET /api/geocode/reverse?lat=&lng=`。

管理：`GET /api/admin/users`、`PUT /api/admin/users/{id}/status`、`DELETE /api/admin/users/{id}`。

所有资源接口必须在服务端校验登录身份和资源归属；管理员接口必须校验管理员角色。

## 5. 核心数据模型

### users

`id, username, password_hash, role(admin/user), status, created_at, updated_at`

### places（景点）

`id, user_id, name, country, province, city, address, longitude, latitude, visit_date, status(draft/published/archived), guide_content, created_at, updated_at`

### foods（美食点）

`id, user_id, place_id(nullable), name, address, longitude, latitude, price, rating, opening_hours, link, notes, status, created_at, updated_at`

### media

`id, user_id, place_id(nullable), food_id(nullable), original_path, thumbnail_path, original_filename, mime_type, size_bytes, sort_order, created_at`

### reviews

`id, place_id, stars, content, pros, cons, recommend_score, enabled_fields_json, created_at, updated_at`

### review_versions

`id, review_id, snapshot_json, version_no, created_at`

### place_links

`id, place_id, title, url, created_at`

## 6. 业务流程

### 6.1 注册登录

注册 → 校验用户名/密码 → 密码哈希入库 → 登录 → 创建会话/JWT → 进入我的地图。

### 6.2 景点录入

点击“新增景点” → 填写名称并搜索 → 选择候选地点 → 地图微调坐标 → 填写日期 → 上传图片并生成缩略图 → 填写攻略 → 新增/关联美食 → 填写评价 → 保存草稿或发布 → 返回地图并显示点位。

任一步骤可保存草稿；草稿继续编辑时从上次步骤恢复。

### 6.3 美食录入

点击“新增美食” → 填写店名/地址 → 搜索或确认经纬度 → 可选关联景点 → 补充图片和结构化字段 → 保存草稿或发布 → 地图显示独立美食点。

### 6.4 筛选查看

进入我的地图 → 输入名称/攻略/店名或选择国家、省、市、日期 → 后端查询当前用户匹配内容 → 更新列表和地图 → 悬浮查看预览 → 点击进入详情。

### 6.5 编辑评价

进入详情 → 编辑评价字段 → 保存当前评价 → 写入 `review_versions` 快照 → 详情页可查看历史版本列表。

### 6.6 归档与删除

内容管理 → 选择归档 → 地图隐藏、管理列表保留；或选择删除 → 二次确认 → 永久删除内容及关联资源 → 返回列表。

## 7. 页面清单

1. 登录页
2. 注册页
3. 我的地图页
4. 景点录入/编辑向导
5. 景点详情页
6. 美食录入/编辑页
7. 美食详情页
8. 内容管理列表页
9. 账号设置页
10. 管理员用户管理页（管理员可见）

## 8. 权限规则

普通用户只能读写自己的景点、美食、图片和评价；草稿、已发布、已归档均遵循资源归属校验。管理员可管理用户和全部内容，但不改变普通用户内容的默认私密属性。未登录用户不能访问地图和任何内容接口。

## 9. MVP 验收标准

- 可注册本地用户并登录，管理员和普通用户权限有效。
- 可创建景点草稿，完成向导并上传最多 10 张、单张不超过 20MB 的图片。
- 原图和缩略图均可正常保存、展示和删除。
- 已发布景点/美食能在 Leaflet 地图显示，悬浮有预览，点击进入详情。
- 可按国家、省、市、单日到访日期和关键词筛选。
- 可选择按日期连线或关闭连线。
- 可创建关联或独立美食点。
- 可编辑评价并查看历史版本，不能对版本进行对比或恢复。
- 草稿和归档默认不显示在地图；归档可在管理列表中恢复。
- 删除操作二次确认，并永久清理关联数据和图片。
- 本地 `localhost` 环境可启动并完成核心流程，目标 10 分钟内录入一趟多景点旅行。

## 10. 后续迭代建议

1. PWA + IndexedDB 实现离线查看和待同步草稿；不需要 Redis，SQLite 也不是浏览器离线存储的首选。
2. 手机扫码/局域网上传，或部署对象存储后支持手机直接上传。
3. 好友分享链接、内容可见性、公开主页。
4. 点位聚合、路线编辑、轨迹播放、卫星图和 3D 地图。
5. 全球化地图搜索、PostGIS 空间索引、图片 EXIF 批量定位。
